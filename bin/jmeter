#!/bin/sh
case $# in
    0) java -classpath $CLASSPATH:`dirname $0`/ApacheJMeter.jar org.apache.jmeter.Driver jmeter.properties;;
    1) java -classpath $CLASSPATH:`dirname $0`/ApacheJMeter.jar org.apache.jmeter.Driver $1;;
esac
